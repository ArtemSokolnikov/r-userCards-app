import React from 'react';
import { Typography } from '@mui/material';

const NotificationsPage = () => {
  return <Typography variant="h4">Notifications</Typography>;
};

export default NotificationsPage;
